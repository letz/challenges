require 'spec_helper'

describe VendorMachine do
  it 'has a version number' do
    expect(VendorMachine::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
